FeyApiRefileOpts                                              *FeyApiRefileOpts*

    Fields: ~
        {source}       (FeyApiHeading)
        {destination}  (FeyApiFile|FeyApiHeading)


FeyApi                                                                  *FeyApi*


FeyApi.load({name?})                                               *FeyApi.load*

    Parameters: ~
        {name?}  (string|string[])  specific file names to return (absolute path). If ommitted, returns all loaded files

    Returns: ~
        (FeyApiFile|FeyApiFile[])


FeyApi.current()                                                *FeyApi.current*
     Get current fey buffer file

    Returns: ~
        (FeyApiFile)


FeyApi.refile({opts})                                            *FeyApi.refile*
    Refile heading to another file or heading
    If executed from capture buffer, it will close the capture buffer

    Parameters: ~
        {opts}  (FeyApiRefileOpts)  @return FeyPromise<boolean>


FeyApi.insert_link()                                        *FeyApi.insert_link*
     Insert a link to a given location at the current cursor position

     The expected format is
     <protocol>:<location>::<in_file_location>

     If <in_file_location> is *<heading>, <heading> is used as prefilled description for the link.
     If <protocol> is id, this format can also be used to pass a prefilled description.
     @param link_location string
     @return FeyPromise<boolean>


FeyApiFile                                                          *FeyApiFile*

    Fields: ~
        {category}         (string)            current file category name. By default it's only filename without extension unless defined differently via #+CATEGORY directive
        {filename}         (string)            absolute path of the current file
        {headings}        (FeyApiHeading[])
        {is_archive_file}  (boolean)


FeyFile:reload()                                                *FeyFile:reload*
     Return refreshed instance of the file

    Returns: ~
        (FeyApiFile)


FeyFile:get_closest_heading()                    *FeyFile:get_closest_heading*
     Return closest heading, or nil if there are no headings found
     If cursor is not provided, it will use current cursor position
     @param cursor? { line: number, col: number } (1, 0)-indexed cursor position, same as returned from `vim.api.nvim_win_get_cursor(0)`
     @return FeyApiHeading | nil


                                                  *FeyFile:get_heading_on_line*
FeyFile:get_heading_on_line({line_number})

    Parameters: ~
        {line_number}  (number)

    Returns: ~
        (FeyApiHeading|nil)


FeyFile:get_link()                                            *FeyFile:get_link*
     Get a link destination as string

     Depending if fey_id_link_to_fey_use_id is set the format is

     id:<uuid>::*title and the id is created if not existing
     or
     file:<filepath>::*title

     The result is meant to be used as link_location for FeyApi.insert_link.
     @return string


FeyApiHeading                                                  *FeyApiHeading*

    Fields: ~
        {title}        (string)                heading title without todo keyword, tags and priority. Ex. `* TODO I am a heading  :SOMETAG:` returns `I am a heading`
        {line}         (string)                full heading line
        {level}        (number)                heading level (number of asterisks). Example: 1
        {todo_value?}  (string)                todo keyword of the heading (Example: TODO, DONE)
        {todo_type?}   ("TODO"|"DONE"|"")
        {tags}         (string[])              List of own tags
        {deadline}     (FeyDate|nil)
        {scheduled}    (FeyDate|nil)
        {properties}   (table<string,string>)  Table containing all properties. All keys are lowercased
        {closed}       (FeyDate|nil)
        {dates}        (FeyDate[])             List of all dates that are not "plan" dates
        {position}     (FeyRange)
        {all_tags}     (string[])              List of all tags (own + inherited)
        {file}         (FeyApiFile)
        {parent}       (FeyApiHeading|nil)
        {priority}     (string|nil)
        {is_archived}  (boolean)               heading marked with the `:ARCHIVE:` tag
        {headings}    (FeyApiHeading[])


FeyHeading:reload()                                        *FeyHeading:reload*
     Return updated version of heading

    Returns: ~
        (FeyApiHeading)


FeyHeading:set_tags({tags})                              *FeyHeading:set_tags*
     Set tags on the heading. This replaces all current tags with provided ones

    Parameters: ~
        {tags}  (string[])

    Returns: ~
        (FeyPromise)


FeyHeading:priority_up()                              *FeyHeading:priority_up*
     Increase priority on a heading

    Returns: ~
        (FeyPromise)


FeyHeading:priority_down()                          *FeyHeading:priority_down*
     Decrease priority on a heading

    Returns: ~
        (FeyPromise)


FeyHeading:set_priority({priority})                  *FeyHeading:set_priority*
     Set specific priority on a heading. Empty string clears the priority

    Parameters: ~
        {priority}  (string)

    Returns: ~
        (FeyPromise)


FeyHeading:set_deadline({date?})                     *FeyHeading:set_deadline*
     Set deadline date

    Parameters: ~
        {date?}  (FeyDate|string|nil)  If ommited, opens the datepicker. Empty string removes the date. String must follow fey date convention (YYYY-MM-DD HH:mm...)

    Returns: ~
        (FeyPromise)


FeyHeading:set_scheduled({date?})                   *FeyHeading:set_scheduled*
     Set scheduled date

    Parameters: ~
        {date?}  (FeyDate|string|nil)  If ommited, opens the datepicker. Empty string removes the date. String must follow fey date convention (YYYY-MM-DD HH:mm...)

    Returns: ~
        (FeyPromise)


                                                      *FeyHeading:set_property*
FeyHeading:set_property({key}, {value?})
     Set property on a heading. Setting value to nil removes the property

    Parameters: ~
        {key}     (string)
        {value?}  (string)


FeyHeading:get_property({key})                       *FeyHeading:get_property*
     Get heading property

    Parameters: ~
        {key}  (string)

    Returns: ~
        (string|nil)


FeyHeading:id_get_or_create()                    *FeyHeading:id_get_or_create*
     Get heading id or create a new one if it doesn't exist
     @return string


FeyHeading:get_link()                                    *FeyHeading:get_link*
     Get a link destination as string

     Depending if fey_id_link_to_fey_use_id is set the format is

     id:<uuid>::*title and the id is created if not existing
     or
     file:<filepath>::*title

     The result is meant to be used as link_location for FeyApi.insert_link.
     @return string


FeyApiAgenda                                                      *FeyApiAgenda*


FeyApiAgendaFilter                                          *FeyApiAgendaFilter*

    Type: ~
        string


FeyApiAgendaOpts                                              *FeyApiAgendaOpts*

    Fields: ~
        {filters?}                            (FeyApiAgendaFilter)
        {header?}                             (string)
        {fey_agenda_files?}                   (string[])
        {fey_agenda_tag_filter_preset?}       (string)
        {fey_agenda_category_filter_preset?}  (string)
        {fey_agenda_sorting_strategy?}        (FeyAgendaSortingStrategy[])
        {fey_agenda_remove_tags?}             (boolean)


FeyApiAgendaOptions : FeyApiAgendaOpts                     *FeyApiAgendaOptions*

    Fields: ~
        {from?}  (string|FeyDate)
        {span?}  (FeyAgendaSpan)


FeyAgenda.agenda({options?})                                  *FeyAgenda.agenda*

    Parameters: ~
        {options?}  (FeyApiAgendaOptions)


                                                      *FeyApiAgendaTodosOptions*
FeyApiAgendaTodosOptions : FeyApiAgendaOpts


FeyAgenda.todos({options?})                                    *FeyAgenda.todos*

    Parameters: ~
        {options?}  (FeyApiAgendaTodosOptions)


                                                   *FeyApiAgendaTagsTodoOptions*
FeyApiAgendaTagsTodoOptions : FeyApiAgendaOpts

    Fields: ~
        {match_query?}                       (string)                             Match query to find the todos
        {fey_agenda_todo_ignore_scheduled?}  (FeyAgendaTodoIgnoreScheduledTypes)
        {fey_agenda_todo_ignore_deadlines?}  (FeyAgendaTodoIgnoreDeadlinesTypes)


FeyAgenda.tags_todo({options?})                            *FeyAgenda.tags_todo*

    Parameters: ~
        {options?}  (FeyApiAgendaTagsOptions)


                                                       *FeyApiAgendaTagsOptions*
FeyApiAgendaTagsOptions : FeyApiAgendaTagsTodoOptions

    Fields: ~
        {todo_only?}  (boolean)


FeyAgenda.tags({options?})                                      *FeyAgenda.tags*

    Parameters: ~
        {options?}  (FeyApiAgendaTagsOptions)


FeyAgenda.open_by_key({key})                             *FeyAgenda.open_by_key*

    Parameters: ~
        {key}  (string)  Key in the agenda prompt (for example: "a", "t", "m", "M")


FeyAgenda.get_heading_at_cursor()            *FeyAgenda.get_heading_at_cursor*
    Get the heading at the cursor position in the agenda view

    Returns: ~
        (FeyApiHeading|nil)


FeyPosition                                                        *FeyPosition*

    Fields: ~
        {start_line}  (number)  start line number
        {end_line}    (number)  end line number
        {start_col}   (number)  start column number
        {end_col}     (number)  end column number


vim:tw=78:ts=8:noet:ft=help:norl:
